# labwc.service — superseded by getty@tty1 autologin + ~admin/.bash_profile
# Left in place for rollback reference only. Not installed by boot-optimize.sh.
#
# The autologin approach gives labwc a proper seat0 session via libseat,
# which systemd-service-with-PAMName=login cannot reliably provide.
